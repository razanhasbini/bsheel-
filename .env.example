SUPABASE_URL=https://api.bsheel.app
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here
MEDIA_SIGNING_SECRET=your-r2-media-hmac-secret-here
# NOTE: Admin dashboard Basic Auth is NOT configured here. It is enforced by
# Caddy on the Contabo server via DASHBOARD_USERNAME / DASHBOARD_PASSWORD in
# /root/supabase-docker/.env — see docs/security/SECRET_ROTATION_RUNBOOK.md.
