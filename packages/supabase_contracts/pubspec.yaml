name: supabase_contracts
description: Single source of truth for Supabase table names, columns, statuses, storage paths, and RPC names.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.2.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
