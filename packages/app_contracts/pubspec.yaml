name: app_contracts
description: Domain value constants and API field names shared by the Bsheel clients.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.2.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
