abstract final class AdminLayoutConstants {
  /// Breakpoint at which the shell switches from the drawer layout to the
  /// persistent sidebar. Only layout constant still in use (admin_shell).
  static const double tabletBreakpoint = 1024;
}
